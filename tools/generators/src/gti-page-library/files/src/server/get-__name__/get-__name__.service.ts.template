import { TService } from '@self/api-core';
import { TGet<%= camelCase(name) %>Response } from './get-<%= name %>.schema';
import { injectable } from 'tsyringe';

@injectable()
export class Get<%= camelCase(name) %>Service
  implements TService<undefined, TGet<%= camelCase(name) %>Response>
{
  execute() {
    return Promise.resolve({
      message: 'Hello, world!',
    });
  }
}
